# Update your Endpoint

Currently, you can update the min & max number of replicas configuring the autoscaling range.
At Launch, it will also be possible to update the repository revision, instance type, and task.
